[tool.poetry]
name = "calculator"
version = "0.1.0"
description = "excel calculator module for divaldi"
authors = ["AX-Ray"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
openpyxl = "^3.1.5"
pytest = "^9.1.1"
pre-commit = "^4.6.2"
black = "^26.5.1"
ruff = "^0.16.6"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
